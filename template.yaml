survey:
  date: yyyy-mm-dd
  version: number
  creator: 
    name: free
    email: 
infrastructure:
  acronym: ref
  name: free
  website: 
  domain: list ref 
  URL/IRI of dataset: 
  URL of discovery portal:   
  repositories: list
    URL: F4
    name: free
    kind: list ref 
    allocation: ref F4 
    software: ref F4
    identifier: list
      kind: ref F1
      system: ref F1
      landing page: bool F1
      assigned: ref F1
      provider: ref F1
      includes metadata schema: list ref F2 
    certification methods: list ref
    policies: list ref R1.1
    registries: list ref F4
    persistency-guaranty: ref A1
    access mechanisms: 
      authentication method: ref A1.2
      access protocol URL: A1.1
      access without costs: bool A1.1
      own user database maintained: bool A1.2
      ORCID used in AAI: bool A1.2
      major access technology supported: ref A1.1
      authorisation technique: ref A1.2
      authorization for accessing content needed: bool A1.2
      data licenses in use: list ref R1.1
      data license IRI: R1.1
      metadata openly available: bool R1.1 
    data:
      type name: list ref I1
      preferred formats: list
        format name: ref I1
        metadata types in data headers: list ref I1
      registered data schema: ref I1
      search on data: bool F4
    metadata: 
      schema: list
        URL: 
        name: ref I1
        provenance fields included: list ref F2
      machine readable provenance: bool R1.2        
      categories defined in registries: bool I2
      PIDs included: bool F3
      primary storage format: ref I1
      export formats supported: list ref I1
      search engine indexing: bool F4      
      exchange/harvesting methods: list ref I1
      local search engine URL: F4
      external search engine types supported: list ref F4
      access policy statements included: bool R1.1
      metadata longevity plan URL: 
      machine actionable: bool I1
      IRI of machine readable metadata of dataset: F2A 
    vocabularies: list
      IRI: I2
      name: ref      
      type: ref I1
      topic: ref I1
      specification language: ref I1 
    data management plans:
      specific DMP tools used: ref
      data publishing steps applied: list ref 
      compliance validation service: list ref R1.3
    data processing:
      special data processing steps applied: list ref 
      workflow frameworks applied: list ref
      distributed workflows tools used: list ref
      other analysis services offered: list ref A1
      data products offered: list ref
    fairness: 
      data findability:  
        data findable: bool 
        gaps: list ref
      data accessibility: 
        data accessible: bool
        gaps: list ref
      data interoperability: 
        data interoperable: bool
        gaps: list ref
      data re-usability: 
        data reusable: bool
        gaps: list ref       